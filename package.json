{
  "name": "vibe-system-builder",
  "version": "1.0.0",
  "description": "Automated installation for Vibe System Builder skills",
  "scripts": {
    "install:claude": "mkdir -p ~/.claude/skills && cp -r ./full ~/.claude/skills/vibe-system-builder",
    "install:antigravity": "mkdir -p ~/.agent/skills && cp -r ./full ~/.agent/skills/vibe-system-builder"
  }
}